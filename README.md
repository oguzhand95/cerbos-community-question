# cerbos-community-question

## Requirements

- Docker
- Golang

## Usage

- Run `docker-compose up` to start `cerbos` container.
- Run `make build` to compile all examples to a binary under the `bin/` directory.
- Run any example.
- Run `docker-compose down` to stop `cerbos` container.
